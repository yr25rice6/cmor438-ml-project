# Supervised Learning

This directory collects the dedicated notebooks for the supervised learning
algorithms covered in the course. Each algorithm has its own numbered
subdirectory so the material can be browsed in the order it is taught.

The first three notebooks are available now. The remaining subdirectories are
placeholders for algorithms that will be added in a later step; each of those
folders holds a `.gitkeep` placeholder so the directory is tracked in git.

## Algorithms

1. **Perceptron** *(available)* — `1. Perceptron/perceptron.ipynb`
2. **Gradient Descent** *(available)* — `2. Gradient Descent/gradient_descent.ipynb`
3. **Linear Regression** *(available)* — `3. Linear Regression/linear_regression.ipynb`
4. **Logistic Regression** *(planned)* — `4. Logistic Regression/`
5. **Multilayer Perceptron** *(planned)* — `5. Multilayer Perceptron/`
6. **K Nearest Neighbors** *(planned)* — `6. K Nearest Neighbors/`
7. **Decision Tree** *(planned)* — `7. Decision Tree/`
8. **Random Forest** *(planned)* — `8. Random Forest/`
